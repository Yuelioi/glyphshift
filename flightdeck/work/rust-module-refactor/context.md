# Context

用户允许按代码结构优雅程度拆分，不要求按固定行数阈值机械切文件。

首要原则是保留现有模块对调用方的接口，把复杂实现放到职责明确的内部模块；避免只做转发的浅模块。结构整理本身不改变探针、字典、运行时或适配器行为；若整理过程中暴露出有明确调用证据的接口注册错误，可以一并修复并用现有合同测试覆盖。

仓库中 `software-intro-video` 的未提交修改属于其他工作，本 Work 不触碰。
