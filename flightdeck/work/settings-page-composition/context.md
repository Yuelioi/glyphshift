# 稳定上下文

- Settings 使用全宽 64px 详情标题带，标题内层、错误提示与 980px 设置分组共用受限内容轴；滚动区
  对称预留滚动条空间，标题带与首个分组之间保持 20px。
- 设置项会立即生效并持久化，不增加保存、应用或重置流程。
- Settings 仍是高密度 Operate 表面，不改造成仪表盘、营销 Hero、多列卡片或满宽长表单。
- 支持基准为 1440×900 与 960×640，深色与浅色、中英文都必须保持标题层级、可操作性和无横向溢出。
- 机器截图、运行日志与临时 Playwright 证据只进入忽略的 `local-test/`。
