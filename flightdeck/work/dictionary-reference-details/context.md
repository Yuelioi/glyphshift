# 稳定上下文

## 产品语义

- Dictionary 可同时被多个 Workflow Target 与 Probe Run 引用。
- 删除必须继续阻断，但错误应列出可见名称；内部 ID 不应成为主要恢复信息。
- 工作流与探针是两类独立引用，文案必须分别标识，不能继续笼统写成“被工作流引用”。
- 批量删除时应汇总所选词典的去重引用名称。

## 约束

- Command Error 保持稳定语义 code，由界面语言生成最终文案。
- 不为定位引用而删除、停用或修改任何工作流与探针。
- 测试只使用合成名称；实机证据继续留在 `local-test/`。
