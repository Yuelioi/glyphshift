# 探针与字典流程统一

Status: Open

## Goal

用户只面对软件、字典、探针任务、工作流。取消临时探针/临时字典的产品概念，默认创建普通字典，支持已有字典和多个排除字典，统一 JSON/CSV 导入导出。

## Current

字典大列表卡顿修复：详情原先以全部条目加新增行为页大小（550 条显示 551），页码固定且没有处理分页更新。现详情、本地列表、在线目录统一默认 50，可选 50 / 100 / 200；详情仅渲染当前页，新建行不计入条目总数，搜索重置页码、删减后夹紧页码，跨页保留草稿。重复原文校验改为一次建立计数索引，避免逐条扫描整本字典。550 条合成回归先复现一次渲染 550 行，再通过分页、翻页编辑、末页、搜索与跨页重复校验；21 项字典回归及补充边界测试通过，前端类型检查和构建通过。当前桌面 App 尚未重建为本轮修复。

最新交互按用户修正：字典详情采用分组操作菜单（设置、导入 JSON/CSV、导出 JSON/CSV），单文件直接打开对应格式保存窗口；列表移除行级导出，仅在选择后提供批量导出。批量一次选择格式和目录，每份字典独立文件，放入唯一的新子目录；显示成功数量与部分失败，取消目录不写文件。此前格式弹窗方案已被本轮替代。20 项字典 Playwright 回归通过，原生 CSV 写出沿用已验证实现。


用户审阅补充：AI 配置添加按钮移入分区标题栏；软件路径下只显示非空厂商与版本；字典列表和详情共用 JSON/CSV 导出弹窗，导入按钮提示两种格式。详情导出已保存内容，有草稿时提示先保存；CSV 正确处理引号、逗号和换行，并附 UTF-8 BOM。后端 13 项合同、5 项相关界面回归及 2 项最终界面确认通过，前端构建、类型检查和界面检测通过。当前已打开的 App 尚未重新构建为本轮改动。


适配器及刷新已提交 `9f670c0`，保留实验标识，等待用户另 2～3 个游戏验证。本轮完成普通任务创建、旧任务保留迁移和停止运行语义；创建失败仍执行内部回滚。排除字典配置持久化，在分页、AI 与导出前过滤已有译文的原文，字典换代会触发列表更新。JSON/CSV 导入支持空译文，默认同原文覆盖，也可保留已有或替换全部；先完整校验再写入。

核心 25 项、桌面后端 82 项、Playwright 探针及刷新 25 项通过，TypeScript 检查通过。使用 `scripts/review-app.ps1 -UseUserData` 构建并启动最新 App，13 个适配器的生产加载器校验通过。构建保留现有第三方 ABI/链接器警告。所有本机证据留在忽略目录。

## 本轮界面与导入补充

用户继续要求图标本地打包、AI 配置去重、权限说明简化、操作按钮悬停说明，以及字典列表和详情页 JSON/CSV 导入。已接入 Nuxt UI `icon.clientBundle` 扫描 Vue/TypeScript，并安装其默认图标所需的 Lucide 集合。新增共用导入确认界面：后端先校验文件，CSV 再补名称和语言，JSON 携带元数据；详情页追加保留同原文、覆盖更新同原文、替换清空现有条目且需明确勾选。详情页继续遵守草稿保存契约。

本轮验证：43 项字典、设置、AI 和新增导入 Playwright 测试通过；3 项文件解析/预检测试通过。断网拦截下图标正常显示且无外部图标请求。指定 review 脚本重新构建并启动最新 App，生产加载器验证 13 个适配器。提交前活动包回归 511 通过、0 失败、25 忽略。用户已授权提交本轮修改。

## 探针列表停止

用户要求无需进入详情即可停止当前探针。已在运行中及暂停中的列表行增加停止按钮，复用原有停止接口；按钮带 hover 说明与忙碌状态，成功后保留任务和字典。修复列表没有选中任务时停止错误被过滤的问题，仍保留详情切换时旧错误隔离。相关 Playwright 4 项通过，包含列表停止失败后重试、两种活动状态、列表保留及既有详情回归。指定 review 脚本已同步构建并启动最新 App，13 项生产加载器校验通过。提交前 `scripts/test.ps1` 活动包回归 512 通过、0 失败、25 忽略。

## Next

等待用户真机验收新建/停止、旧任务与字典保留、排除字典和导入导出。当前排除规则只针对已有译文；若用户要求包含空译文，应调整过滤规则及说明。CatSystem2 与 VGUI 保持实验状态，跨游戏验证继续在适配器 Work 中记录。
