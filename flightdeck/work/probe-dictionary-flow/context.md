# Context

用户明确取消“临时”概念；停止运行保留探针、软件及字典。新建默认创建新字典，可以使用已有字典。排除字典可多选，不写入排除字典；默认按其已有译文过滤待处理原文，已向用户异步确认是否还需包含未翻译原文。导入导出只展示 JSON/CSV，不使用“绑定字典”“联合表”等内部术语；导入默认同原文覆盖，可选择保留已有或替换全部。旧 quick 数据需安全保留，失败创建的内部回滚与产品资产生命周期分开。
