# 实施计划

1. [完成] 收集、AI、导入按附加字典原文排除，提交重检，旧重复不自动删除。
2. [完成，破坏性升级] 新任务存储不加载旧任务/启用状态，软件与字典保留；后端只允许单软件。用户授权放弃旧数据，取消迁移实现。
3. [入口返修已验证；待用户桌面验收，剩退出/重连验证] 统一工作流列表、收集详情与控制，独立探针导航/创建接口移除，写入切换隔离，删除记录保留资产。
4. [已实现，字号实验待真机验收] 字典字体与工作流默认分别继承；50–200% 比例及独立 FontScale 能力，GDI 三入口原生像素/恢复合同通过。
5. [进行中] 中文帮助与领域文档/README 已同步当前入口；相关原生/页面通过。旧产品 UI 回归改写、真实软件字体字号及最终桌面验收待做。

6. [已实现并通过页面回归，待桌面验收] 移除独立软件大类，工作流第一步设置程序、自动命名、可清空最近记录；添加字典默认左侧新建、右侧已有。
