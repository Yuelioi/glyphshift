# 单软件工作流统一

Status: Open

## Goal

合并探针与工作流为单软件工作流，统一收集、翻译与运行；多字典指定写入位置，附加字典按原文排除；字典字体覆盖工作流默认字体，字号按比例并由适配器单独声明能力。

## Current

用户已确认本轮界面并授权提交。兼容性免责声明的安全保护图标使用红色，卡顿/异常图标使用黄色，开始前及异常说明均提醒先保存工程；中英文同步。提交仅包含已验收桌面功能，未完成的 Qt 翻译上下文研究保留在工作区。隔离暂存树的前端构建通过；首次引导、设置、帮助与工作流生命周期共 28 项页面检查最终通过，另 3 项列表与标题检查通过。活动包检查的两个 worker 握手测试在并行执行时超时，该合同独立复测 8 项全部通过；没有修改其实现或扩大超时。旧独立软件管理页面测试仍待迁移到工作流内选择路径，未将其计入本轮通过结果。字体页补齐二级标题，保持视觉样式。提交前暂存隐私检查通过，本机材料与未完成研究均未纳入。

首次设置导览已接入统一引导：兼容性确认后先进入设置，依次介绍匹配管理员权限、AI 配置、文字处理、最近软件、字体和语言；这些介绍只需下一步，不强制改设置，再切到真实创建工作流步骤。设置分区由应用与 SettingsView 的 section model 共享，推进按步骤身份定位，不使用旧硬编码序号。首张卡片按自身中心定位到距顶部约 40%（距底部约 60%），随卡片高度和窗口尺寸重新定位；此坐标依据用户“高一点”的表述，若用户明确指顶部 60% 可直接调整。设置中新增常驻“查看兼容性与免责声明”，复用首次弹窗，查看关闭不重置确认状态或引导进度。Playwright 6 项通过，覆盖设置先于工作流、各步真实高亮、中英文入口、免责声明重开以及两个宽度的弹窗边界；相关截图已检查。首次同源 review 启动遇到原生初始化异常：保存的管理员启动偏好触发 UAC 期间，WebView 已请求快捷键 IPC，但 WorkflowShortcuts 尚未 manage，直接 state() 查询造成 panic。两个快捷键 IPC 已改为 try_state()，未初始化返回 runtime unavailable，包级编译通过；现有测试没有无状态 AppHandle 的 IPC seam，不能将空过滤测试计作回归。修正版已通过 `scripts/review-app.ps1 -UseUserData` 同源重建，16 个适配器经生产加载器及配套目录校验；普通启动进程退出并交接到新管理员进程，新进程持续运行且响应，未再出现初始化 Rust panic。旧管理员窗口仍需用户自行关闭，未提交。

首次引导返修：指示鼠标置于提示卡之上；高亮按实时 DOM 边界外扩 8px，软件与字典选择步骤覆盖完整弹窗（含标题和确认按钮），字典设置页签覆盖完整 trigger。仅引导打开时逐帧跟踪弹窗动画、滚动和尺寸变化。提示卡传送到页面浮层，选择步骤改为上方定位，避免弹窗裁剪和窄窗口右侧溢出；显式恢复 pointer events，弹窗中可跳过且保留当前配置窗口。无界面文字提醒的后端门槛与后台状态刷新周期均缩为 5 秒，中英文文案同步，收到文字后继续自动清除。后端定向回归先失败后通过；相关界面 11 项通过后，新增裁剪/点击检查定位出窄窗口溢出，修正后的双宽度回归 2 项通过，截图已检查。前端构建通过；已按用户要求使用 `scripts/review-app.ps1 -UseUserData` 同源重建并启动最新桌面，16 个适配器经过生产加载器及配套目录校验，沿用原有用户数据，进程启动后仍在运行。未提交，本机证据仅留在忽略目录。

提交前验证：显式活动包入口 `scripts/test.ps1` 完整通过，554 项通过、0 失败、25 项按声明忽略，归档 UIA/OCR 不在构建和测试范围。暂存路径和文本隐私检查通过，本机证据未进入提交。

最新交付为可扩展字典导入导出：后端 `dictionary_transfer` 以注册表隔离 JSON、CSV、SRT 处理器，公共解析结果包含词条和可选字典信息；错误保留位置参数，不依赖桌面、文件系统或工作流。JSON/CSV 支持编码，SRT 仅提取。桌面桥只读取受限 UTF-8 文件并映射错误，前端文件服务负责多文件预检和文件名错误汇总，弹窗负责创建方式、语言和词条合并。

支持混选文件分别创建或合并；单独创建支持统一语言及逐文件修改，合并同原文可选前/后文件优先、空译文不覆盖非空。已有字典入口支持按格式多选，沿用追加/覆盖/替换。全部预检成功后才写；创建中途失败保留已完成数量，同一弹窗重试不重复创建，尚未完成的文件设置可修改。导入不是跨文件事务，关闭弹窗不会回滚已创建字典。清空字典已改为直接弹窗确认，不输入名称。

原生格式测试 3 项、桌面解析桥 6 项、后端合同 19 项通过；导入导出页面 7 项、前端构建通过，两个导入页面截图已检查。[模块职责与扩展方式](references/dictionary-transfer.md)。已按用户要求通过 review-app 同源重建并启动最新桌面，使用原用户数据，运行包 13 个适配器经生产加载器校验通过。未提交。PotPlayer 缓存方向已由用户明确取消，后续采用提前导入 SRT、翻译字典再启动目标的使用流程，不宣称支持字幕实时热更新。

本轮新改动：未选字典时默认创建，已有字典后添加默认已有；跳过规则移至全局设置“文字过滤”，旧默认 AI 配置规则一次迁移，后续不随 AI 配置切换。字典可隐藏跳过项而不删除，桌面分类复用 AI 的原文规则并在后台批量计算；结果按原文/规则变化更新，分页复用。设置与 AI 规划读取同一全局策略，自动补全内容签名包含全局策略。桌面 API 为 35，旧壳需同源重建。当前运行的桌面仍是上一轮版本，未自动重启。

验证：设置/迁移 Rust 10 项、AI 单元/过滤合同 10 项、新工作流字典默认 3 项、字典隐藏页面 1 项通过，自动补全边界 6 项通过，跨页面用例改为字典导航后定向复测通过（自动补全共 7 项）；前端构建通过，设置截图已检查。未提交。

驻留组件限制已核对：停用没有完整释放部署，改变绑定或库集合被 InvalidDeployment 拒绝，安全热卸载尚未实现，不能强制 FreeLibrary 替代。PotPlayer 后续交给文字覆盖 Work，尚无漏采字幕红例。

工作流设置已接管软件入口：标题栏不再显示软件大类，新建/编辑第一分区为设置软件，浏览文件、运行列表、快捷捕获及最近记录统一到选择弹窗。确认后自动填工作流名称，手改名称保留；换目标保留字典和字体、重新选择兼容方式。最近记录最多 20 项，旧绑定首次作为列表来源，清空只清历史不破坏工作流。添加字典弹窗默认左侧创建，右侧已有。帮助、README、产品与领域约定同步。

相关 Playwright 13 项通过（10 项收集/字典/字体/入口回归及 3 项新设置流程），前端构建通过；软件选择和新建字典弹窗截图已检查。已按用户要求通过 review-app 同源重建桌面及 Runtime Bundle，13 个适配器经生产 loader 校验通过，使用原用户数据启动；进程有窗口且响应正常，启动错误日志为空。未提交。程序接入校验仍复用既有原生路径，不修改运行与适配器实现；浏览器预览不支持真实收集，新建落盘通过本地模型验证，创建后直达收集由桌面命令 mock 回归验证。

上一轮非适配器核心审查的六项修复仍保留，见 [核心审查与修复报告](review-core.md)。

工作流合并入口已返修：名称/双击和“收集与翻译”进入收集详情，新建成功直达；运行、暂停、继续、停止、刷新、编辑、导入导出、AI/自动补全复用原收集页面。详情单独打开工作流设置；没有 writer 时解释配置，不隐藏入口；打开错误在当前页面可见。用户尚未完整验收。

- 工作流只含一个软件，使用新任务存储与内部所有权记录；用户授权旧任务不迁移、不加载。软件、字典、AI 配置仍保留。未物理删除旧任务文件。
- 字典展示仅列已选项；添加弹窗支持已有/新建，单选 writer、排序、移除。一本自动写入，移除 writer 选择剩余第一本，排序不改 writer。附加字典按原文存在排除 writer 新增，不依赖译文；手工/批量/AI/导入重检。
- 收集与翻译共用工作流 Runtime，会话控制归属工作流；切换 writer 隔离记录，运行中改配置先停止。删除工作流删除所属记录，保留资产。设置“数据”可打开当前字典目录。

字体与字号本轮已实现：

- 字典元数据新增可选有序 fontFamilies 与 fontScalePercent，创建/编辑/保存/重开/JSON 往返保留；CSV 仍只含词条。严格写入限制字体数量/名称、比例 50–200；本地坏字体偏好恢复默认，不丢词条。
- 工作流 fontPolicy 选择保留原字体、统一默认或 preferDictionary；默认字号 100%。译文胜出字典决定字体偏好，字体与字号分别继承；字典显式 100% 覆盖工作流缩放。应用范围沿用已翻译文字/全部观测文字。
- core/workflow 按适配器分别生成字体规则；FontScale 是独立能力，不借用 FontSubstitute。FontRule/FontDecision 支持 Scaled；FontPolicy 增加单适配器词条规则，摘要与 Runtime JSON 完整覆盖。Runtime publication schema 为 3，桌面 API 为 34。
- NativeDecisionV1 保持 C 结构布局，新增字号决策位与上部比例位；原生 GDI 三个适配器支持独立缩放，绘制前复制 LOGFONT，保留高度正负语义与自动宽度，绘制后恢复和释放。符号字体仍不改；控件尺寸不自动改变。ExtTextOut 缩放时不沿用旧字距。
- TextOutW、DrawTextW/DrawTextExW、ExtTextOutW 的字号能力标为实验；VGUI、CatSystem2、其他引擎未宣称支持。其他已有字体替换适配器继续按自己的能力接收字体规则。

验证：字体优先级、独立继承、能力隔离、JSON 往返/非法比例、导入导出、坏本地偏好修复通过。原生 GDI 三个 DLL 合成绘制在 150%/75% 产生不同像素，100%/停用恢复。前端类型检查与构建通过，字典/工作流/刷新/自动补全累计 31 项页面测试通过（两处旧探针导航测试改为工作流路径后定向复测）；合成页面截图已检查。

公共协议变更触发活动包回归。修复 test.ps1 采用显式 package 列表并排除归档 UIA、OCR 及其依赖者。活动回归 525 项通过、25 项忽略，4 项旧多软件工作流 fixture 失败；改写成多个单软件工作流后 runtime 全部 34 项通过，覆盖共享字典换代、停止隔离和冲突替换。没有构建/测试归档 UIA 或 OCR worker。

review-app 已完成同一 checkout 的 Release 桌面和 Runtime Bundle 重建，13 个适配器经生产 loader 校验通过，用户数据模式启动成功。未提交。

启动反馈与重试返修：resume_probe_run 现在转发 Runtime 错误；进一步定位到工作流收集启动失败会消耗连接，但 Pool 仍复用失败实例，下一次变成 InvalidState 并显示笼统启动失败。set_features_with_collection 在无活动会话的启动失败后清理实例、收集配置与请求特征，重试重新发现；有活动会话仍保留。合成工作流失败后重试测试先失败后通过，Runtime 全部 35 项通过。真实目标诊断返回过 TargetRuntimeRestartRequired，后续有 RemoteThreadUnavailable，需用户保存工程并完整重启目标后验收，不代替用户关闭目标。临时诊断已移除。

收集页面补齐 [50, 100, 200] 分页，旧 20 条偏好恢复 50；后端 ProbeQuery 上限同步从 100 改为 200，200 接受/201 拒绝边界与 capture 全部 27 项测试通过；页面验证选项及 200 条选择通过。桌面启动错误、重试与完整启停页面回归通过。最新构建仍走 review-app 的同源桌面/Runtime 和生产 loader 校验；未提交。

收集结果与列表返修：所属工作流的观察记录不再补入 writer 全字典的 Unobserved 行；空观测结果保持空，观测后才联结 writer 译文，附加字典排除保持不变。状态用待翻译/已翻译/已忽略，旧记录的 Unobserved 改为尚未检测到。capture 28 项通过，新增空观察+字典已有词条的回归先失败后通过。工作流把兼容方式数量放到软件下方，移除多目标/并行冗余文案，操作列分配真实宽度，窄桌面截图检查通过；页面 9 项通过，最终布局定向复测通过。尚未重启发布给用户，未提交。

周期闪烁返修：桌面每 5 秒调用 refresh_workflows，Pool::refresh_workflow 原先无条件 stop/discover/start，导致短暂恢复原文，并通过普通 start 丢失收集配置。改为保持所有权校验后 reconcile：不变的活动会话保持连接，仅配置/译文变更应用更新。回归先确认轮询增加 discovery 次数而失败；修改后连续轮询不重新发现，TextObserve 和收集控制仍可用。Runtime 36 项通过，补充收集保持定向测试通过。真实目标退出后自动发现重启仍待单独完善，不能继续用定时拆建会话实现存活检查。

自动补全间隔改为 0–60 秒，默认仍 10 秒；设置前后端/持久化/帮助同步。0 取消额外调度等待：启用时立即检查，此后 250ms 检查新内容；空候选按观察数量/字典修订/排除修订跳过重复规划，已有任务时等待，取消勾选防止晚到任务提交。采集本身仍有约 1 秒 checkpoint，不承诺 AI 零延迟。边界设置 Rust 测试通过，自动补全与设置 5 项页面测试通过，前端构建通过；没有重启当前 App，未提交。

工作流界面收紧：软件路径下过滤厂商/版本的纯横线等占位值；列表字典改为数量，悬停展示名称；操作保留收集与翻译、设置、更多，诊断移入更多菜单，缩小固定操作列。软件设置仅展示当前软件，搜索选择移入弹窗，确认才变更，取消不影响原配置。页面 9 项通过，悬停断言调整后定向复测通过；前端构建通过，窄窗口列表与选择弹窗截图已检查。未重建或重启桌面，未提交。

收集界面与字典设置调整：工作流名称列吸收剩余宽度，操作列保持按钮所需宽度，1100/1600 视口验证不拉伸且可点击；已选字典的写入与优先级说明移入问号浮层；字典字体从手输改为可搜索本机字体的多选列表，保留导入字典的已选字体，虚拟化列表。运行软件/强制刷新文字进入任务操作，自动补全成为 AI 补全旁的独立勾选项，帮助同步。remoteThread 管理员错误不再推断为受保护软件，明确旧会话可能驻留及保存工作、完整退出目标后重开；刷新 SessionRejected 以动作上下文显示更新未接受和恢复步骤，保留原始错误码。相关 15 项页面用例经修正及定向复测通过，前端构建通过；字体搜索保存与列表/收集截图已检查。未重新构建桌面、未重启 App、未提交。

连接状态与 AI 测试修正：通用 target_access_failed 不再统称权限不匹配，显示连接失败；明确 targetProcess 不可用时归为软件未启动。窗口关闭不等于后台进程退出，不据此杀进程或把所有无窗口目标排除。字典帮助按钮与标题使用 flex 居中。连接测试原先沿用默认 30 分钟和重试预算，现由任务层对 connection scope 临时限制为不超过 30 秒、0 重试，正常翻译与持久化配置保持不变。先失败的回归确认旧实现会重试 3 次，修复后通过，协议 7 项通过；状态区分与问号页面 3 项通过，截图检查与前端构建通过。尚未重建或重启桌面，未提交。

连接测试重复执行返修：复现任务轮询响应乱序会用旧 running 覆盖 completed，导致 taskRunning 误锁测试按钮；任务快照按请求序号接收，新任务启动也使此前查询失效。新增独立 testingProfileId 覆盖规划至结束，模型/配置保存后清除旧连接报告。回归先失败后通过，页面验证连续测试、换模型重新测试和旧查询不覆盖完成状态；设置自动补全间隔使用统一 compact 右侧控件列并加时钟图标，与其他控件对齐。2 项定向页面验证与前端构建通过，截图检查通过。未重建或重启桌面，未提交。

字典批量操作补齐：表头全选覆盖所有分页，有搜索时只选择匹配项，半选态可见；字典操作增加清空字典，弹窗要求输入字典名称二次确认，忽略搜索清空全部词条、保留元数据，仍按草稿/保存流程落盘。字典与工作流 AI 启动移除自动打开任务页，手动查看任务入口保留。跨页/筛选全选、错误名称禁止清空、取消保留词条、清空保存以及 AI 启动留在当前字典后手动查看批次各定向用例通过；前端构建与清空弹窗截图检查通过。未重建或重启桌面，未提交。

设置说明与术语统一：compact 设置行将剩余宽度分配给左侧文字，控件保持右侧统一列，分区说明取消窄行限制；移除静态快捷键当前值重复行，录制/冲突/保存反馈保留。中文资源统一字典称呼，测试界面选择器同步，用户自定义名称不迁移。新增可复用 FieldHelp，自动补全间隔、工作流全局快捷键和字典字体/字号的长说明放入可悬停与点击的帮助浮层，短说明直接显示。前端构建通过，工作流术语/帮助验证通过，设置不重复快捷键与单行短说明定向验证通过；页面截图已检查。未重建或重启桌面，未提交。

说明改为日常用语：字典问号分成“写入 / 其他字典 / 优先级”三段，保留按原文排除（空译文也排除）和从上到下取译文的规则。同步简化 AI 配置、自动补全、字体、快捷键、刷新等主要设置说明，中文继续统一叫字典，不改用户名称。前端构建通过，设置帮助与字典选择定向 Playwright 3 项通过。未重建或重启桌面，未提交。

CSV 空译文重导入返修：实际阻断来自重复原文，不是空译文或编码。共享 JSON/CSV 导入解析改为合并原文与译文完全相同的行，保持首出现顺序；不同译文仍拒绝并返回独立冲突提示，避免静默丢失。合成回归先失败后通过，导入解析/文件预览 4 项通过，用户文件通过本地实际解析与去重验证，前端构建通过。原始文件未修改；未重建或重启桌面，未提交。

应用更新提示：接入官方 release 元数据端点，原生 HTTP 请求限时 10 秒、响应最多 64 KiB；使用 SemVer 优先级比较，构建标签不制造更新，下载地址必须 HTTPS。启动时默认检查，自动失败静默；设置可持久关闭或手动检查。顶部紧凑可关闭提示提供纯文本更新说明与前往下载，不自动下载安装；关闭开关作废迟到结果。当前下载域名纳入 opener 权限。前端构建、版本与地址合同 1 项、设置合同 7 项、Playwright 提示/下载/关闭/持久开关/迟到响应 2 项通过，设置截图已检查。未重建或重启桌面，未提交。

CSV/JSON 导入诊断：移除解析错误统一折叠；共享导入入口返回 CSV 物理行号及引号、表头、列数、空原文、冲突双方位置，JSON 语法行列与结构/词条编号，文件读取、编码和大小独立原因。保留严格解析和重复相同行合并，不修改输入文件。合成反馈回归先失败后通过；导入合同 6 项、页面错误提示与无写入测试 1 项通过，原始输入的实际解析器回放定位正确；前端构建通过。用户文档同步错误反馈说明。正在使用 review-app 同源重建并按用户要求启动，未提交。

工作流目标语言返修：桌面软件状态沿用固定中文默认值，导致合法的中文转英文字典在运行解析时被 LocaleMismatch 拒绝。编译输入现从写入字典（未指定时第一本所选字典）取得目标语言，同时考虑字典预览 override；无字典时保留原字体流程默认值。不同目标语言的多字典仍按原组合合同拒绝，错误文案改为字典之间的目标语言冲突。回归先失败后通过，桌面后台合同 17 项、前端构建通过；没有修改用户字典或工作流数据，也未连接目标软件。桌面尚未重建或重启，未提交。

## Next

本轮引导与 5 秒兼容性提醒已启动最新桌面，等待用户验收；后续用户要求构建或启动最新桌面时只使用 `scripts/review-app.ps1` 同源重建 Shell 与 Runtime Bundle。先检查创建按钮、软件选择、字典设置页签及添加字典弹窗的高亮与提示卡，再确认无文字时的提醒和收到文字后的清除。保留其他未提交工作，不自动连接真实目标。

AE 崩溃已取得用户转储：异常为 stack overflow，GDI/文字整形路径的重复返回地址超过两千组。发现 CallbackGuard 使用 bool::then_some(Self)，拒绝重入时临时 Guard 仍被构造并 Drop，错误清除外层线程保护标记。统一改为惰性 then(|| Self)，覆盖同模式的原生/框架适配器；暂停的 console/direct2d 仅修源码，未构建测试，未涉及归档 UIA。

精确故障 DLL 的合成原生重入测试出现 4 次翻译回调（预期 1）而失败；修复 DLL 同测试仅 1 次，通过。GDI guard 单元回归先失败后通过；原生 GDI、常见 GDI、GDI+ 与字号替换/停用恢复通过。该证据定位防递归缺陷，但真实 AE 重启后仍需用户验收，不能把合成回归当作真机完全通过。转储、地址和原始证据仅在本地。修复版已通过 review-app -BuildOnly 同源重建，13 个适配器经生产 loader 两次校验通过；未启动桌面或自动连接真实目标，待用户完整重启目标后验收。

按 [计划](plan.md) 验收当前桌面。真实软件字号像素、长文本裁剪与停用恢复，以及 x86 字号像素仍需验收；实验能力不能直接晋级正式。其他适配器字号支持另按入口实现验证，不能只扩 Catalog。

剩余合并验证：退出末批收集落盘、暂停后目标重启；旧独立探针/多目标 UI 测试尚有未改写部分。字体配置现已实现，不再沿用“字体尚未开始”的旧交接结论。启动最新 App 始终只用 review-app 脚本。
