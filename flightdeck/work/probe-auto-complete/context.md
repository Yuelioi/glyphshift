# 上下文

用户需要在游戏中持续翻译新出现的文字，明确选择设置页调整检测间隔、探针 AI 菜单勾选自动补全。此授权替代自动循环中每轮确认的旧规则，手动补全仍需确认。开关是会话状态，不作为设备启动时的持久授权；间隔属于普通全局设置。

自动循环复用后端完整探针计划、过滤与 CAS 写回。前端只调度，不复制翻译或字典匹配逻辑。单次异步检测不可重入，异步等待后重新检查开关与目标状态。失败或取消后停止，不创建无限重试；取消勾选只阻止后续任务，已提交任务在任务页停止。
