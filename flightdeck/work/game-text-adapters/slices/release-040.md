# v0.4.0 发布

Status: Validated; ready to tag

发布本轮字典规则元数据、规则行编辑与分组、捕获清空修正、多适配器协同、退出清理、AI 模型列表、托盘置顶、列表排序和返回导航。应用 package、Tauri、Cargo 与锁文件版本统一为 0.4.0。

只提交本轮产品、测试、文档和当前 Work；其他视频 Work 修改及本地缓存不属于此次提交。暂存路径和新增文本已检查，不含本机绝对路径或本地证据。

前端生产构建通过。发布前活动包测试 588 项通过、26 项忽略；使用显式活动包脚本，排除归档 UIA/OCR。计划提交 main，创建并推送 v0.4.0 标签，触发现有 Windows 发布流水线。
