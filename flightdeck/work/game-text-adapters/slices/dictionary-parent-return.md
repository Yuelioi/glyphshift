# 字典返回入口

Status: Implemented; review app launched

从工作流文字页打开字典时记录父工作流，返回按钮指向原工作流，恢复原筛选/分页状态。从字典库进入则返回字典库。父工作流不存在时回到字典库。

返回操作复用未保存修改确认，继续编辑保留页面，放弃更改仍返回原入口。按钮文案与实际目标保持一致。

前端生产构建通过，2 项 Playwright 测试通过：工作流往返、筛选恢复、返回时取消/放弃修改，以及字典库原有返回流程。评审脚本已同步构建启动，两份 Bundle 各通过 13 个适配器校验；窗口响应、哈希一致、标准错误日志为空。
