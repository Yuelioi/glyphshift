# 稳定上下文

## 产品语义

- 字体目录用于查找和勾选本机字体，其可见顺序应在一次浏览过程中保持稳定。
- `fontPolicy.families` 是有序字体候选集合，顺序仍代表优先级。
- 当前已选区域按优先级展示候选，并承载调整顺序与移除操作。

## 约束

- 搜索和“仅已选”过滤只改变可见目录，不改变候选顺序。
- 使用现有工作流编辑状态与 Nuxt UI primitive，不建立第二份字体选择状态。
- Playwright 只使用合成字体；视觉证据留在 `local-test/evidence/`。
