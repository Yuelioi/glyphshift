# 持久本地测试根计划

- [x] 冻结 `local-test/` 持久本机工作区与 `target/` 可清理缓存的边界。
- [x] 更新 Git ignore、AGENTS、产品文档、脚本、Playwright 和 Rust 本地合同路径。
- [x] 将现有机器配置、授权样本、证据与可复用数据迁入新根，确认旧根不再含唯一持久文件。
- [x] 运行 PowerShell 入口语法、Runtime wire 隐私合同与 Playwright 配置定向检查。
- [x] 自审 tracked/staged 路径与文本，完成 Work 并恢复实时翻译覆盖主线。
