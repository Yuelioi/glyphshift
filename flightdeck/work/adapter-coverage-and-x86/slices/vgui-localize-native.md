# VGUI 查询原生接入

展开 Plan 第 20 项；当前实机目标仍是《求生之路》的 VGUI 文字适配。

## Current

用户已实测确认自动刷新可用，明确要求标为“能用，但未完全验收”。保留实验状态，待其他 VGUI 游戏扩大覆盖；当前提交此阶段实现后转向下一游戏的对话捕获。

本轮已接入标准 Label 控件刷新：接口/成员/槽位从真实函数推导，RTTI 验证完整 owner 和 TextImage；保留原本地化索引，UI 线程随代次调用目标 setter。句柄序号及最后文字校验支持隐藏恢复、销毁/复用拒绝和游戏外部改字保留。停用等待失败不报成功。未知形态仍为查询回退，描述符改为 RetainedObject。

原生 DLL 的同主菜单两代与恢复通过内存/画面/像素断言；生产 Backend/Runtime 发布及停用合同连续两次通过，画面确认同一主菜单可见更新。部分生产截图受其他窗口遮挡，生产四帧像素门禁未完成，不能据此扩大覆盖结论。6 项新定向测试和 7 个查询进程合同通过，Clippy 通过。最新输出回退修正避免超长/错误新译文留下旧译文，已通过指定脚本同步重建并启动最新 Release App，生产加载器验证 12 个适配器，窗口正常响应。以下为查询阶段的历史记录。

最新用户修正：停用其他来源后能看到 VGUI 完整文本，单字不再处理；目前重点是已配译文词条的部分可见替换失败。最新探针完整文本为 318 条。下一步按 [扩大可见翻译覆盖](../references/vgui-visible-translation-coverage.md) 调查控件写入、存量对象更新及恢复；下述 3 条完整文本快照属于早期采样。

用户从最新 App 复测主菜单仍不完整：当前探针的 3 条完整文本来自查询 Adapter，29 条单字来自 GDI；4 个截图可见主菜单标签的完整采集断言失败。此前一个探针仅有 172 条 GDI 单字。该结果确认混合来源与缺失发生于展示之前；当前快照不足以单独证明是初始化缓存还是控件绕过查询。不能靠禁用 GDI 或隐藏所有单字宣称修复。

新增 `windows.vgui.localize-query` 原生实现，使用公开接口工厂加函数数据流识别名称/索引查询，不携带单次实验的槽位、地址或词句。完整查询走既有 V1 Host 和 Core 字典；返回存储按原文/译文复用，1 MiB / 4096 项为进程内实例的硬上限，换代与启停不重置。

`scripts/test-vgui-localize.ps1` 的 7 个独立进程合同通过：两种对象/槽位布局、两种错误 ABI/表达式拒绝、项数预算、字节预算、重入/错误/代次/停用等待。两个正例经过生产 Runtime 字典发布与捕获路径。新包 x86 Clippy 严格检查通过。

同一识别器对先前保留的完整目标接口表离线检查通过，与此前人工确认的入口一致。随后在新的授权目标实例中，通过生产 Desktop Backend 创建字典/工作流，由 Runtime/Controller 原生发布，完成单个设置标签的首代、次代和停用重建恢复。两代各有 2 次替换命中，三阶段图像人工核对及像素掩码断言通过；原始/恢复交并比约 0.996，首代/次代约 0.630，原始/首代约 0.208。该检查不是 OCR，不代表整个菜单覆盖。

`scripts/test.ps1` 活动包回归 505 项通过、0 失败、25 忽略。使用同 checkout 重新构建的本地实验 Bundle，添加新 x86 Adapter 后经生产加载器验证；目标中加载的 Runtime 与 Adapter 路径/散列也与该 Bundle 对应。此前实验包有 12 个逻辑 Adapter；用户要求从 App 测试后，默认打包配置也加入了 x86 查询 Adapter，以实验选项提供窄能力。

生产实机合同正常结束并确认 Runtime 停用；关闭、重新打开设置页后恢复原文，再取消设置页回到主菜单。游戏仍正常响应，未结束目标进程，未修改安装文件或设置。原生影子表、模块与返回池按设计留驻，等待目标正常退出。原始词句、机器输入、图像、日志、散列和失败输出全部留在本地测试目录。

## Steps

- [x] 原生查询数据流识别与未知形态拒绝。
- [x] V1 Host/Core 字典、返回池、重入和停用等待接入。
- [x] 独立 C++ 与生产 Runtime 合同，离线检查已调查目标的查询形态。
- [x] 活动包回归与同 checkout 的本地实验 Bundle 验证。
- [x] 新目标实例中的原生首代、次代、停用后重建恢复及像素断言（单个设置标签）。
- [x] 将 x86 查询 Adapter 加入默认打包及 App 可选列表，说明重建要求与旧主菜单限制。
- [x] 含实验选项的同步 Release App / Bundle 构建、生产加载器验证并启动供用户测试。
- [ ] 实际 App 列表及用户操作验收：Playwright CLI 因 WebView 调试端口拒绝连接未完成，用户手动反馈主菜单仍缺完整文字，需补齐该用例。
- [x] 标准 Label 存量对象发现、同菜单换代及停用恢复。
- [ ] 扩大设置标签、下拉选项与非标准控件的实机覆盖。

## Next

使用同步构建的最新 App 让用户复测原先必须重启才生效的标签，记录仍不能写回的类别。一次升级需正常重启已有驻留旧工件的游戏；支持范围内的后续字典修改不需重启。保持实验标识，不把标准 Label 成功扩大为任意 Source 分支、格式化文本或并发可写调用者均兼容。App GUI 的 Playwright CLI 检查仍被本机 WebView 调试端口拒绝连接阻断；加载器与真实 Runtime 验证不能代替 GUI 检查。

实现及资源/恢复边界见 [Adapter 说明](../../../../crates/adapters/implementations/framework/vgui-localize-native/README.md)；原始证据仅本地保存。
