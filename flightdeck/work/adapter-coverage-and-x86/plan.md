# 双架构 GDI 实施

1. [x] 架构识别失败拒绝部署、同位数远程地址解析；定向构建 x86 Controller / Runtime / 三项 GDI。
2. [x] 一个 Adapter ID/版本下的架构工件、双架构清单和同位数元数据生成/目标复验。
3. [x] 在现有 Controller 传输接口下路由不同架构目标，保留混合进程族及独立启停语义。
4. [x] 同一 x64 App 的双架构 GDI 合成验收、故障拒绝和 x64 回归。
5. [x] 通过规定入口同步打包并启动 App，交由用户验收指定 x86 软件；真实软件结果待用户测试。

以上为已验收并提交的 GDI 首批。后续用户授权的扩展见下方；仍不运行归档 UIA/OCR。

## 其他活动适配器双架构扩展

用户已验收首批并要求继续补齐其他适配器。范围为默认 Runtime Bundle 的活动适配器；新增游戏引擎文字入口不等同于现有 Adapter 的架构移植。

6. [x] GDI+ / DirectWrite：x86 原生替换、字体与恢复合同，接入 Bundle。
7. [x] GTK 3 / raylib：C ABI 与结构体传值检查，双架构原生合同和工件。
8. [x] Qt Painter：Qt 5 MSVC x86 thiscall、导出及容器布局；Qt Quick 根据受支持 Qt 版本保留明确边界。
9. [x] MonoGame / Unity Mono：Profiler / Mono 调用边界、架构门控、托管测试宿主。
10. [x] App 入口与活动包回归、同步打包启动，记录每项实际通过的范围及仍缺的条件；真实框架软件待用户验收。

## 通用文字完整性

11. [x] Domain 共用完整性解析：完整文本、可靠运行段片段、显式结束/取消、字形来源；禁止无依据拼字与追溯替换。
12. [x] 可选 Native Text Host 扩展及 Runtime 调用范围；兼容 V1、清理跨生命周期片段、抑制从属字形的重复观察/替换。
13. [x] Qt Painter / DirectWrite 接入，并用生产 Qt + GDI + Runtime 的 x86/x64 嵌套绘制合同验证共用行为。
14. [x] 同步打包并启动最新 App，记录共用机制与未接入框架的实际覆盖范围。
15. [ ] 将 VGUI 等框架的完整文字/对象入口接到共用契约，并验证样本软件中的整句采集、更新与恢复；不使用游戏专用地址或拼词规则。

    VGUI 延迟绘制、派生 Paint、公开 Surface 上下文配对均有通过的独立宿主合同。实机 PrintText 决策及测量接受不等于可见替换，前台截图尚未变化；公开上下文已覆盖未归属的主菜单逐字调用，仍须定位状态失效导致的回退。最新光标推进模式的独立合同通过，实机菜单尚未复验。用户已暂停推进，先完成整理；新适配器未进入默认 Bundle/Catalog。


## 暂停后的社区调研

16. [x] 对比成熟翻译项目的提取/内嵌机制，以及 VGUI 本地化与控件接口；形成路线复核。调研未恢复实机测试，也未完成第 15 项。后续优先评估完整文本所有者，逐字重放保留为实验后备。

17. [x] 上层入口只读调查和独立缓存生命周期模型；x86/x64 各 6 项通过。该完成项不是原生接口接入或实机验收。
18. [ ] 可信本地化查询 ABI 接入、新建控件真实替换/更新/恢复；明确历史控件刷新或初始化覆盖方式后，才评估正式产品接入。


    第 18 项进展：只读查询探针及新建设置界面的对应已通过，9 个可见标签取得完整查询结果；第 19 项完成其中一个标签的 Frida 受控替换，第 20 项的原生 Slice 随后完成入口自动识别及同一窄范围的原生替换、字典更新与停用重建恢复。历史控件刷新和更广覆盖仍未完成，因此本项保持未完成。查询 Adapter 已按用户要求作为实验选项加入默认 Bundle。


## 查询层受控替换

19. [x] 在已对应的单个设置标签上验证可见首代、第二代与停用重建恢复；独立宿主覆盖返回指针生命周期和预算，实机截图/状态断言收口。
20. [ ] 正式查询 Adapter 产品化：可信 ABI 能力、Core/字典发布接入、长期返回内存策略、控件刷新/初始化覆盖。第 18 项的正式接口与更广覆盖要求仍未整体完成。

    [VGUI 查询原生接入](slices/vgui-localize-native.md)：原生识别、Core 字典决策、有界返回池、独立合同及单个设置标签的原生实机三阶段验证已完成；默认 Bundle 已加入查询实验选项；标准 Label 历史控件刷新已实现并获用户确认可用；更广覆盖尚未验收，因此第 20 项仍不勾选。

## CatSystem2 正文入口

21. [x] 已启动目标的正文捕获调查：现有产品探针缺失正文的基线成立；独立观察器取得完整 UTF-8 字符串及文字对象，正文重建两轮复核通过，历史记录也取得最初台词。仅完成捕获可行性，见 [调查记录](references/catsystem2-dialogue.md)。
22. [ ] CatSystem2 原生适配器：可信命令/对象入口识别、生产采集、替换存储与对象生命周期、字典更新和停用恢复，补齐合成合同与实机可见验证。

    [CatSystem2 原生 Slice](slices/catsystem2-native.md)：实现、独立宿主合同、默认 Bundle、同步 Release App 已完成；旧目标的生产加载检查要求重启，真实采集及可见替换/更新/恢复待验证，因此本项保持未完成。
