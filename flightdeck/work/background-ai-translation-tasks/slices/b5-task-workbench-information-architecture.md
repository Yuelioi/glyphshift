# B5 翻译任务工作台信息架构

## Deliverable

把拼接式长页面重构为正式任务工作台：默认突出当前任务与停止动作，统计和历史各归独立 Tab，低频
批次与历史详情按需展开；标题栏入口不再像界面语言切换。

## Steps

- [x] 使用横向 Tab 分离当前任务、统计和任务列表，并保留键盘与窄窗口可用性。
- [x] 当前任务首屏突出目标、整体进度、耗时、写入与停止；未报告用量只说明一次。
- [x] 批次默认折叠，重试/失败自动展开；任务历史支持筛选与单条详情展开。
- [x] 标题栏换为任务清单图标，活动批次仍保留紧凑计数。
- [x] 更新中英文、定向 Playwright、机械 detector、两视口截图和同步桌面 App。

## Current

任务工作台已按使用频率正式分层。当前任务默认打开，停止为右上角明确危险动作，整体进度与四项运行
事实优先；Provider 未完成批次时只显示一次用量等待说明。统计和任务列表分别进入独立 Tab，历史支持
搜索、状态筛选和单条详情展开。标题栏入口改为任务清单图标并保留批次计数。

## Evidence

- `npm run build` 通过；AI 翻译定向 Playwright 11/11，全套 Playwright 94/94。
- Impeccable mechanical detector 返回空 finding。
- 当前任务 960×640/1440×900、统计 1440×900、任务列表 1440×900 均无横向溢出；独立 review PASS。
- `scripts/review-app.ps1 -Profile Release` 成功，最新进程响应正常、嵌入页面加载且 stderr 为空。

## Next

None.
