# 工作流快捷键与探针批量删除

Status: Finished

## Goal

工作流菜单仅显示复制/删除；支持混合临时探针的批量删除；工作流可录入、保存全局快捷键并切换启停。

## Current

后续反馈已处理：编辑工作流保存成功后不退出编辑页，不重置当前分区、软件目标或字体搜索；
只刷新已保存内容、revision 与未保存基线。新增连续保存两次、保留第二个目标、保存后 Esc
正常返回的回归，工作流与快捷键相关 15 项 Playwright 通过。留页修正已通过同步 Release/Runtime
审阅构建，并按用户要求使用原用户数据启动；新窗口正常响应。

三项已完成：工作流菜单只显示复制/删除；探针批量删除可混合普通与临时任务，预检查后复用
临时资产 ownership 清理；工作流基础配置支持点击录入全局组合键，Esc 取消、Backspace 清空。

快捷键随工作流定义持久化，旧文件默认未配置，复制清空绑定，保存冲突不覆盖原快捷键，
删除注销，启动恢复。系统事件复用既有启停与互斥规则，异步执行后通知前端；长按和并发触发
有防重复处理，录入期间屏蔽全局动作，快速捕获不会抢占已绑定的工作流组合键。

验证：桌面 shell 84 项、backend 合同 13 项通过；相关 Playwright 套件覆盖 26 项既有页面
回归及 30 项探针/设置/快捷键回归，最后对新增 3 项和紧凑录入布局再次通过。
隔离本机数据与合成 Native 目标实测：未聚焦启停、连续按住仅触发一次、冲突拒绝、失败保存
回滚、复制不继承、清空/删除释放、重启恢复、录入屏蔽均通过。所有原始证据仅在本机忽略目录。

此前三项功能的正常可见配置 Release shell 与同 checkout Runtime Bundle 已通过同步构建和 9 个 Adapter
生产加载校验，构建就绪。隐藏验证进程已结束，未替换正在运行的已安装版；本次仅做本地提交，不推送。

## Next

None。功能与验证已完成；用户若要求审阅启动，继续使用同步审阅脚本及原用户数据。

## References

- [上下文](context.md)
