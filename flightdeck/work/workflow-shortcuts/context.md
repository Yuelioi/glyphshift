# 工作约束

- 用户要求短菜单文案，确认删除对话框仍可显示对象名称。
- 快捷键通过点击区域录入；Esc 取消、Backspace 清空，不能把普通打字框当成快捷键配置。
- 快捷键绑定到单个工作流，调用既有启停逻辑，不绕过目标占用和探针互斥；按住按键不反复切换。
- 复制工作流不复制全局绑定，防止冲突；删除工作流注销绑定；启动恢复绑定，冲突应可见。
- 临时任务删除复用 ownership 清理，复用/被引用资产保留；活动普通探针与 AI 写锁需要先检查。
- 不改动归档 UIA，验证使用显式活动包与仓库 Playwright；所有实机输入输出留在本机忽略目录。
- 本次不包含提交、打 tag 或 push。
