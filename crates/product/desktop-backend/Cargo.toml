[package]
name = "glyphshift-desktop-backend"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
glyphshift-adapter-registry = { path = "../../adapters/platform/registry" }
glyphshift-dictionary-distribution = { path = "../../dictionary/distribution" }
glyphshift-dictionary-package = { path = "../../dictionary/package" }
glyphshift-domain = { path = "../../core/domain" }
glyphshift-runtime-contract = { path = "../../runtime/contract" }
glyphshift-translation = { path = "../../core/translation" }
glyphshift-workflow = { path = "../../core/workflow" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tempfile = "3"

[dev-dependencies]
sha2 = "0.10"
tempfile = "3"

[lints]
workspace = true
