[package]
name = "glyphshift-translation"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
glyphshift-domain = { path = "../domain" }
regex = "1"
serde = { version = "1", features = ["derive"] }

[lints]
workspace = true
