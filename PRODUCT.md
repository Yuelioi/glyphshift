# Glyphshift 产品契约

## 目的

Glyphshift 让用户在不修改软件安装文件的前提下，为多个桌面软件持续应用界面文字和字体替换，
并管理软件、词典、Adapter Catalog、探针与运行工作流。当前交付平台是 Windows。

## 用户

- 需要使用中文或其他目标语言操作桌面软件的用户。
- 维护软件界面词典与字体替换策略的作者。
- 为新软件或新渲染路径提供 Extension、Controller 或 Capability Adapter 的开发者。

## 主要任务

1. 从运行中软件列表、按键捕获或手动程序路径新建软件，通过基础接入预检后确认名称、用途说明和
   程序位置；软件资料页只提供一个统一新建入口。
2. 创建独立词典，维护语言、发布元数据和纯文字规则。
3. 创建工作流，为每个软件目标分别配置 Adapter Plan、有序词典和可选内联字体策略。
4. 在字体策略中维护有序字体候选，并选择仅修改词典命中文字或 Hook 捕获的全部文字。
   本机字体目录跨启动复用缓存，只有用户明确刷新时才重新扫描系统字体。
5. 启用工作流并查看持久期望、实际 Runtime 状态、Generation 与可恢复错误。
6. 从帮助页查看本机 Runtime Bundle 提供的 Adapter 名称、平台、技术、能力、配置要求、版本与官方
   技术文档；文档使用 Adapter presentation 提供的 HTTPS 地址并由系统默认浏览器打开。
7. 新建或恢复探针任务。新建时目标可来自软件资料库，也可从运行中可见软件列表选择或用快捷键捕获
   当前前台程序；词典可来自已有资料库或系统自动命名的临时空词典。两项来源彼此独立，不要求用户
   先跨页面准备资产。
8. 由 Glyphshift 在一次原子创建中解析或创建 Software、Dictionary、兼容 Adapter Plan 与 Probe。
   任一临时资产存在时可整体保留为正式资料库资产，或只清理由本次创建拥有且未被引用的资产。

## 产品承诺

- 工作流、软件、词典和 Runtime actual state 是四种不同事实，不互相冒充。
- 只有目标 Runtime 对写回能力 ACK 后才显示实际生效；进程发现或 Observe 不算翻译成功。
- 文字替换和字体替换可以独立产生，也可以组合。
- 字典和 Adapter 可以独立复用；字体候选与覆盖范围只存在于 Workflow Target。
- 在线字典的便携 metadata 与下载来源、摘要、签名和安装状态严格分离。
- 新增使用既有能力的软件不修改 Core、Desktop 或 GUI 源码。
- 多进程软件由 Software Extension 显式声明后代可执行文件 allowlist；Controller 只接纳属于授权根
  进程树的实例，任一成员退出或失败不覆盖其他成员事实。
- 所有失败路径 fail-open，不终止或远程卸载用户正在工作的目标进程。
- 普通用户只需按名称与能力选择 Adapter，无需理解 Controller、Route、进程标识或 DLL。
- 设置页不配置在线翻译服务；当前词典是本地版本化资产，未来由网站或字典市场提供下载和安装。
- 应用首次启动使用深色主题；用户可在标题栏一键切换明暗主题，也可在设置中选择深色、浅色或
  跟随系统，并在 `zh-CN` 与 `en-US` 界面之间切换。设置还提供当前用户开机启动、“最小化到
  任务栏 / 彻底退出”关闭行为，以及“始终以管理员身份启动”开关。该开关默认关闭；开启后立即
  请求 UAC 重启，并在之后每次启动时继续请求，直到用户关闭。软件快速捕获快捷键也属于设备级设置：
  用户单击当前组合键后直接按键录制，系统探测可用且真实重绑成功后才保存，不提供自由文本编辑。
- 桌面后端跨 IPC 返回稳定语义错误码与类型化参数；最终人类可读文案由当前界面语言决定。
- 工作流实际状态必须区分软件未启动、权限不匹配、组件加载/兼容失败和启动超时等可执行原因；
  不使用“需要处理”一类无法指导恢复的汇总文案。多软件工作流保留逐目标错误详情。
- 添加软件先确认程序绑定是可访问且正在运行的 Windows PE，并阻断重复绑定、自身目标、当前
  Runtime/Adapter 缺失和目标架构不支持等确定失败；Adapter 是否覆盖真实绘制路径仍须在添加后
  通过激活或探针证据确认，不能在添加时凭名称猜测。
- 软件统一从“新建软件”弹窗进入，可选择运行中可见程序、使用两段式全局按键捕获或浏览/手动输入
  路径；三种方式都只填充确认表单，不自动保存，也不绕过同一套接入预检。
- 软件支持行级与批量删除；仍被 Workflow 或 Probe Run 引用时必须保留记录并说明引用数量与解除
  方法，不静默失败、不级联删除相关资产，也不改变现有 Runtime 状态。
- Runtime Bundle `/3` 只接受产品内置的第一方 authority，并在加载 Native artifact 前逐件验证清单
  路径与 SHA-256；Release Bundle 不携带合成测试宿主。清单
  authority 不等同于在线签名或证书服务。
- Windows 安装候选嵌入已验证的 Release Runtime Bundle；未配置代码签名时明确标记为本地
  unsigned candidate，不自动安装，也不冒充公共发行版。
- Dictionary 是唯一翻译内容资产；Probe Run 只保存任务状态与可修改的 Dictionary 引用，Observation
  Index 单独保存可恢复的技术证据。二者在探针详情联合展示，但观测不会自动变成词条；只有用户填写
  非空译文或显式同步已翻译的多选条目时才写入绑定 Dictionary。从词典移除译文不删除观测证据，
  切换绑定也不复制或删除新旧词典内容。
- Probe 只提供具备 `TextReplace` 的产品 Adapter；纯观察器实现可保留在源码中，但不进入产品目录、
  默认计划或 Runtime Bundle。
- Probe 创建来源不建立第二套软件、词典或探针模型；持久 ownership ledger 只负责临时资产的崩溃
  恢复、失败补偿、保留和安全清理。复用资产与被 Workflow/其他 Probe 引用的资产永不随临时会话删除。
- Dictionary 编辑使用一份显式草稿：设置、行内词条、新增与删除共享未保存状态；只有保存成功才
  更新持久基线，离开编辑器或关闭窗口前必须保护未保存更改。
- Dictionary 新建与设置复用同一元数据表单。名称、说明、标签、源语言和目标语言常显；发布版本、
  作者、许可证与主页收在“更多”中。标签与作者使用独立项目输入，发布版本由主/次/修订三个非负
  数字生成。

## 明确不做

- 不提供旧产品数据迁移或兼容模式。
- 不以具体软件品牌组织 Core、GUI 或固定页面。
- 不在软件资料页配置文字/字体功能。
- 不在 Dictionary metadata 或 entry 中保存字体、Hook、Adapter、平台或运行配置。
- 不提供独立 Font Profile、字体导航或无法由 Hook 识别的 Location/Region 配置。
- 当前不实现账号、Catalog 上架、网络上传和自动更新。在线目录未配置时明确离线；本地“发布”只
  导出可重新导入的标准 Dictionary `/2` JSON 文件。
- 不把桌面后端可用性显示成常驻“已连接”状态；桌面组件不可用属于需要明确处理的产品错误。
- 不把本机路径、截图、日志或真实软件测试数据作为仓库资源。
