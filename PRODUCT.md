# Glyphshift 产品契约

## 目的

Glyphshift 让用户在不修改软件安装文件的前提下，为多个 Windows 桌面软件持续应用界面文字和
字体替换，并在一个产品里管理软件身份、可复用词典和运行工作流。

## 用户

- 需要使用中文或其他目标语言操作桌面软件的用户。
- 维护软件界面词典、字体替换与语境规则的作者。
- 为新软件或新渲染路径提供 Extension、Controller 或 Capability Adapter 的开发者。

## 主要任务

1. 添加软件名称、用途说明和程序位置。
2. 创建独立词典，选择语言和适用 Hook，维护文字与字体规则。
3. 创建工作流，为一个或多个软件目标配置有序词典集合。
4. 启用工作流并查看持久期望、实际 Runtime 状态、Generation 与可恢复错误。

## 产品承诺

- 工作流、软件、词典和 Runtime actual state 是四种不同事实，不互相冒充。
- 只有目标 Runtime 对写回能力 ACK 后才显示实际生效；进程发现或 Observe 不算翻译成功。
- 文字替换和字体替换可以独立产生，也可以组合。
- 新增使用既有能力的软件不修改 Core、Desktop 或 GUI 源码。
- 所有失败路径 fail-open，不终止或远程卸载用户正在工作的目标进程。
- 普通用户无需理解 Adapter、Controller、Route、进程标识或 DLL。

## 明确不做

- 不提供旧产品数据迁移或兼容模式。
- 不以具体软件品牌组织 Core、GUI 或固定页面。
- 不在软件资料页配置文字/字体功能。
- 不把本机路径、截图、日志或真实软件测试数据作为仓库资源。
