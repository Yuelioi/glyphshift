# Glyphshift 产品契约

## 产品定位

Glyphshift 是面向 Windows 桌面软件的运行时界面翻译工具。它让用户在不修改目标软件安装文件的
前提下，发现界面原文、维护可复用词典，并通过持续工作流应用文字和字体替换。

Glyphshift 不把“找到进程”“捕获文字”“生成替换决策”和“最终画面可见”混为一件事。产品通过
探针和运行状态把这些阶段分别呈现，让兼容性可以被验证，而不是靠软件名称猜测。

## 用户

- 需要把 Windows 桌面软件界面从现有语言翻译为所需目标语言的用户。
- 维护、校对和复用软件界面词典的译者与工具作者。
- 为新的界面技术路径提供 Capability Adapter 的开发者。

## 用户成功路径

### 第一次完成界面翻译

首次启动先用轻量兼容性说明建立正确预期：大多数常见桌面软件与正常使用场景可以直接使用，再说明少数自绘界面、安全保护或特殊运行环境可能不兼容，以及停止工作流、重启目标软件或更换适配器的恢复方式。不得用高风险措辞把少数兼容性例外描述成默认结果。

首次使用采用可跳过、可从设置重新运行的交互式引导。引导直接锚定真实产品控件，用聚光、高亮、箭头或指针提示当前动作；需要用户操作的步骤只有在用户实际点击或完成对应操作后才推进，不创建与真实产品分离的教程页面。兼容性确认完成后，重新运行引导不重复要求确认。

1. 先介绍设置：部分目标需要匹配管理员权限，然后依次介绍 AI 配置、文字处理、最近软件、字体和语言。仅介绍，不强制填写 AI 配置或改变偏好。
2. 新建工作流，第一步设置软件：从最近记录或运行中软件选择、快捷捕获，或浏览程序文件。
3. 选择兼容方式并添加字典：弹窗左侧默认创建新字典，右侧选择已有字典。
4. 打开目标菜单、对话框和面板，在工作流收集界面确认原文与可用 Adapter。
5. 手工填写译文，或使用 AI Profile 补全仍为空白的候选。
6. 在目标界面确认效果后，保留这个工作流，之后直接运行即可。

### 长期维护翻译

- 独立编辑、导入、导出和复用词典，不把技术证据写进词典内容。
- 为每个工作流目标选择 Adapter、有序词典和可选字体策略。
- 查看工作流期望与 Runtime 实际状态，按具体错误恢复软件、权限或组件问题。
- 在软件升级或界面变化后重新使用探针验证覆盖。

## 产品表面

### 工作流

工作流是默认管理页，表达持续运行期望。每个目标独立组合软件、Adapter、有序词典与可选字体策略；
启用工作流就是请求 Glyphshift 持续维持这份组合，没有第二个“开始翻译”动作。
每个工作流可配置一个全局快捷键，窗口未聚焦时也能切换启用/停用；点击录入区域监听组合键，
Esc 取消、Backspace 清空，保存工作流后生效。复制工作流不继承快捷键，冲突时保留原绑定并提示。
编辑工作流保存成功后停留在当前分区和软件目标，便于反复调整字体并查看效果；返回列表由用户主动操作。

### 设置软件与最近记录

不提供独立软件管理页。工作流第一步设置软件：浏览程序文件、选择运行中的程序、快捷捕获和最近记录共用程序校验与绑定。选择软件后自动填写工作流名称；用户手工修改的名称不会被后续选择覆盖。

新建工作流直接打开软件选择窗口。点击工作流的设置软件页签时，若尚未选择软件，也自动打开软件选择窗口；已选择软件但尚未选择字典时，点击字典页签自动打开添加字典窗口。取消后不反复弹出，重新点击空配置页签可再次打开；已有配置只展示当前设置。

最近记录只用于快速选择，最多保留 20 项，可以清空。清空不删除工作流依赖的程序绑定，也不停止运行；旧绑定作为首次最近列表来源，用户无需单独维护软件资料。内部稳定绑定仍用于运行授权和防止同一程序被多个工作流同时占用。

### 词典

词典是唯一翻译内容资产。它保存便携元数据、待翻译条目和完成译文；可选字体候选与字号比例随 JSON 保存；Adapter、观测来源与运行状态不进入词典。词典编辑使用一份受保护草稿，保存成功才更新持久内容。JSON/CSV 导入先校验文件，创建时补充元数据；详情页可追加、覆盖同原文或明确确认后替换全部词条，导入仍需保存草稿。

### 工作流收集

收集、翻译与运行都属于工作流，不提供独立探针入口。每个工作流只能选择一个软件，可复制工作流服务其他软件。
工作流字典展示区只列出已选字典，支持排序、移除及单选写入位置。“添加字典”弹窗左侧是创建新字典，右侧是已有字典。未选字典时默认新建，已选字典时默认已有。只选一本自动设为写入字典；多本也保持唯一写入位置，移除当前写入字典后自动选择剩余第一本。排序不改变写入位置。
其他所选字典只要包含原文就排除收集，即使译文为空；收集和 AI 写回重检此规则，已有重复不自动删除。
新原文去重追加到写入字典。切换写入字典使用独立观察记录，避免把旧原文灌入新字典。
详情提供暂停/继续收集、停止运行、刷新目标文字、导入导出和 AI 补全；自动补全间隔仍在设置中。
停止运行保留字典。删除工作流删除内部观察记录，保留软件和字典；AI 写入期间禁止删除。
此次为破坏性升级：旧工作流、启用状态和探针记录不再读取；软件、字典和 AI 配置保留，不执行旧任务迁移。

### 设置

“缺字备用字体”允许按译文语言保存一个本机字体，提供常用语言选项并支持自定义语言代码。配置支持搜索、修改和移除；它属于缺字时的候选，不覆盖工作流默认字体及其中每个字典的显式字体选择。当前设置已支持持久化，运行时消费随各引擎的缺字回退能力接入，页面应如实说明尚未支持的状态。

设置页使用通用、AI 配置、文字处理、软件管理、字体管理、语言管理六个局部导航，不增加应用主导航。AI 配置页单独管理可复用 AI Profile 与自动补全间隔；通用页按外观、软件更新、快捷键、应用与权限、数据等主题分区，其中本机字典数据入口位于最底部。软件管理页维护最近记录，支持逐条删除和清空，与工作流选择器共享；不删除已有工作流的软件绑定。字体管理页管理可搜索的本机常用字体和缺字备用字体，常用字体在字典、工作流及备用字体选择器中优先显示。语言管理页管理翻译语言候选，内置 20 种常用语言，支持搜索、自定义代码、删除和恢复内置列表；移除候选不修改已有字典或备用字体配置。字典新建、编辑及导入复用同一语言选择器，仍允许手动输入。

全局文字处理通过“捕获黑名单”管理过滤，不保存替换规则。正则替换规则作为字典元数据 `textRules` 保存，在字典设置中编辑、启停、排序和测试，随字典包导入、导出和发布。按工作流字典顺序先匹配规则，规则仅查所属字典；`{{TR}}` 代表第一捕获组的译文，`$2` 原样拼回数值。收录和自动翻译只处理拆出的固定文字，不累积数值词条。规则命中但译文缺失时保留完整原文，不回退到整条译文；无规则匹配时才执行原有完整词条查找。规则修改随字典修订更新运行与采集缓存，历史词条不自动清理。没有全局规则迁移或兼容路径。单条测试不读字典，不保存，以用户填写的模拟译文验证捕获组和结果。

通用页包含外观、软件更新、快捷键、应用与权限和本机数据等分区，所有偏好立即应用并保存在当前设备。
AI 配置页单独管理可复用 AI Profile 与自动补全间隔；请求策略随 Profile 一起切换。

### 翻译任务

翻译任务是标题栏可随时打开的全局后台任务。Glyphshift 同时只运行一个任务，任务内批次按所选 AI 配置的并发上限执行。手动任务不自动排队；工作流自动补全会按工作流合并等待项，轮到时重新读取最新待译内容，不缓存过期翻译计划。间隔 0 保留每 250 毫秒检查，取消勾选移除对应等待项，重启不恢复自动付费任务。用户可以
切换到工作流、软件、其他词典或探针继续工作。任务页显示批次、写入数量、耗时、供应商实报 Token、
最近记录和按 Profile/模型汇总的统计。

### 帮助

关于页提供在线文档和软件发布页面链接，通过系统默认浏览器打开。

帮助页分为使用指南、AI 翻译、故障排查、技术与兼容和关于五个 Tab。默认指南用“新建工作流 → 设置软件
→ 添加字典 → 开始运行 → 收集原文 → 填写译文 → 验证效果”的完整路径带用户完成第一次翻译；其他 Tab
分别解释后台 AI 任务与 Token、按现象恢复问题、按需展开的 Adapter 技术信息，以及当前版本、项目
GitHub 仓库和作者主页。

## 产品承诺

### 资产与状态分离

- 软件身份、词典内容、工作流期望、Adapter Catalog 和 Runtime 实际状态是不同事实。
- 工作流选择保留原字体、统一默认或优先字典字体；字体与字号分别继承。AI Profile 只属于 AI 翻译连接与请求策略。
- 探针保存任务与观测证据，译文始终写入其绑定词典。
- 在线词典发布、目录展示、下载描述和本地安装状态彼此分离。

### 安全与可恢复性

- 默认工作区位于当前用户 Roaming AppData 的 `Glyphshift/workspace`；bundle identifier 不作为用户数据
  文件夹名。显式本机测试根保持隔离。
- 本地持久内容按字段、记录和文件三级隔离错误：未知字段忽略，非法非核心字段恢复安全默认，坏记录只
  跳过自身，语法损坏文件保留原件并允许其他工作区内容继续加载。
- 所有目标内失败路径保持 fail-open，不因 Glyphshift 失败而阻断目标软件原调用。
- 暂停探针先满足本地暂停意图；连接失效时允许在目标恢复后重新发现。
- 成功创建的探针、软件和字典分别保留；复用或被其他对象引用的资产不会被误删。
- 删除软件或词典遇到引用时保留对象，并说明需要先解除的关系，不做静默级联删除。
- 未保存的词典与配置草稿在离开页面或退出前受到保护。

### 可解释的运行状态

- 工作流统一使用开始/停止。目标未启动或自然退出时显示等待软件启动；主动停止后不自动重连。顶部按状态计数，列表、详情读取同一运行快照，历史文字不能证明目标还在运行。
- 工作流开始运行并启用新原文收集后，如果连续 10 秒没有收到任何新的界面文字观测，显示兼容性提醒但不停止工作流；后续一旦收到界面文字，提醒自动消失。
- 收集新原文是持久化工作流设置，不是另一套生命周期。关闭时保留已有译文和字体替换，目标重开仍沿用设置。无写入字典不能开启收集。
- 停止失败继续显示并保留会话归属，可再次停止，不能删除仍活跃的工作流。刷新失败显示状态待确认，旧回包不得覆盖新状态。
- 进入工作流及返回前台触发刷新；后台受控对齐不重启未变化的活跃连接。无目标继续等待，明确的配置/权限/驻留错误等待手动处理，激活超时采用有限次数退避重试。
- 自动补全在目标自然退出后暂停新轮次、允许当前轮完成并保留选择；主动停止工作流按任务拥有者取消自动任务，其他工作流和手动字典任务不受影响。

- “软件未启动”“权限不匹配”“组件不兼容”“启动超时”等原因分别呈现，不使用笼统的失败汇总。
- 进程发现和文字观测不代表替换已经生效。
- Runtime 生成替换决策不代表绘制 API 已接受调用，也不证明最终像素可见。
- 普通界面按用户可理解的名称与能力展示 Adapter，不暴露进程标识、内部 token 或本地模块路径。

### AI 翻译

- AI 一键翻译只选择仍为空白的候选，保留已有译文和任务期间新增的人工译文。
- 本机过滤默认跳过不适合翻译的数字、尺寸、单字符、URL、邮箱、文件路径与快捷键等内容。
- Profile 拥有供应商协议、服务地址、模型、推理强度、分批、超时、并发和重试策略；翻译默认
  关闭推理，只有用户显式选择时才使用供应商默认或更高强度。
- 新建 Profile 默认单批超时 30 分钟；前端不再用更短的固定总截止时间覆盖 Profile 策略。
- API Key 按用户选择随 AI Profile 明文保存在本机配置文件中；设置表单默认遮蔽并支持显式显示，且明确
  提示仅在受信电脑上使用。词典、任务日志和错误仍不得复制 Key。
- 编辑 Profile 会载入当前 API Key，保存时直接替换本机配置；删除 Profile 会同时删除其中的 Key，
  不依赖 Windows Credential Manager 或另一个隐藏的凭据生命周期。
- 取消会停止后续请求与写入，丢弃迟到结果，并保留已经完成的译文。
- 每次请求前先显示待翻译文本数和批次数；原文输入粗估明确排除输出、推理、缓存与重试。
- 每次终态任务在本机保留有界、脱敏的翻译运行记录，供测试排查批次、尝试、耗时与供应商 usage。
- 词典与探针 AI 翻译由后台任务持有；目标词典在任务期间只读，其他产品表面不锁定，完成批次立即
  写回并保留。
- 同一时间只允许一个活动任务；再次发起会打开当前任务，不建立可能继续消耗额度的隐藏队列。
- 任务页只显示供应商实报的输入、缓存输入、输出、推理与总 Token；未返回的用量不估算。
- Codex 订阅 Profile 复用本机 Codex CLI 的 ChatGPT 登录，不读取、复制或保存账户令牌；任务使用
  临时结构化运行，关闭推理使用 Codex 支持的无推理档，默认单批并发。
- 真正退出应用会中断未完成请求；下次启动记录为意外中断，绝不自动重发可能计费的请求。

### 桌面体验

- 当前界面提供简体中文与 English，并支持深色、浅色和跟随系统主题。
- 标题栏提供翻译任务、主题、帮助、设置和原生窗口操作；核心导航只保留工作流、软件、词典与探针。
- 设置支持当前用户开机启动、关闭窗口行为、快速捕获快捷键和管理员启动偏好。最小化可收进托盘；关闭可选收进托盘、最小化或退出。托盘左键恢复，右键提供显示与退出。窗口置顶可通过标题栏或设置切换并记住偏好。
- 正常退出先停止目标运行效果并保存收录末批，成功后关闭窗口；失败则保留窗口、提示重试。退出期间暂停后台重连，持久工作流配置保留。关闭设为最小化时仍继续运行。
- 键盘用户可以发现并聚焦主内容；图标按钮具有可访问名称，状态不只依赖颜色。

## 适用范围与边界

- 当前发布平台是 Windows；覆盖取决于目标实际使用的界面绘制技术。
- Glyphshift 不保证覆盖所有软件、所有窗口或所有文字，兼容性必须用探针和目标界面验证。
- Glyphshift 不是修改安装包的语言补丁，也不是对整个屏幕执行 OCR 后覆盖翻译层。
- 产品不按具体软件品牌建立特殊页面或 Core 分支；新增软件应通过已有通用能力接入。
- 当前不提供账号、云同步、Catalog 上架、网络上传或自动更新。
- 在线词典目录未配置时保持明确离线，本地词典管理继续可用。
- 当前未承诺旧版产品数据迁移；发布前的重要词典应保留导出副本。

### 字体与字号

字体和字号属于工作流，不属于字典内容。工作流提供默认字体候选与默认字号比例；每个已选字典可在该工作流内单独设置字体候选和字号，分别留空时继承默认值，显式 100% 表示原字号。相同原文按字典译文优先级决定使用哪份字典设置。比例范围为 50%–200%，不会自动扩大控件或改变布局。同一字典可供不同工作流独立配置；复制工作流会复制这些设置，删除字典绑定会移除对应设置。

旧字典文件中的字体元数据仅作格式兼容保留，不参与运行时决策；新设置只写入工作流的 `fontPolicy.dictionaryOverrides`，不会回写字典。字典编辑界面不再提供字体或字号表单。工作流内创建字典复用统一语言选择器，支持内置、用户管理和手动输入语言；每个字典的字体选择器支持搜索全部本机字体或常用字体。

字号缩放是独立实验能力，目前实现于 Windows TextOutW、DrawTextW/DrawTextExW 和 ExtTextOutW。字体替换能力不代表支持字号；没有缩放能力时明确提示，不将该能力宣称为 VGUI、CatSystem2 或其他引擎支持。

### 文字处理与收录过滤

不再收录规则归 App Settings，在工作流自动写入新原文前执行，所有 AI 配置共用；旧默认 AI 配置的规则迁移一次后保持全局独立。字典可隐藏被原文规则命中的条目，不删除、不改译文，搜索和分页基于可见条目。原文或规则变化时后台批量分类，翻页复用结果；自动收录、AI 规划和桌面字典分类使用同一 Rust 实现。启动先安装保存的策略，设置变化使收录版本缓存失效；既有字典条目不受追溯删除。数字属性名称不因包含冒号被当成数值单位。


采集列表与筛选性能：状态查询不触发字典追加；观测批次由独立采集检查追加，停止时收取末尾批次，AI 写入锁解除后重试。工作流列表固定应用捕获黑名单，过滤发生在分页之前，不提供“已跳过”筛选或隐藏开关。清空字典同时重置关联工作流的当前捕获记录，停止旧采集写入后清除记录并恢复原运行意图；之后相同原文也正常收录，不保存删除排除集合。来源提示只显示额外字典，写入字典不重复标注。翻页和搜索复用当前观测及字典版本的组合快照，慢请求期间的连续操作合并为最新一次；AI 计划使用一次固定快照。字典仍按批次原子保存，缓存不改变字典优先级、原文排除或译文归属。

新建工作流选择软件后，默认勾选全部可选适配器；重新选择同一软件保留用户已取消的项目，编辑已有工作流不自动补勾。界面统一使用“适配器”，并说明其用途是读取和替换软件文字。
